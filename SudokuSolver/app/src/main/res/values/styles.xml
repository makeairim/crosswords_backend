<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="textAppearance">?android:textAppearanceMedium</item>-->

    </style>

    <style name="MyActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Dark</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="actionButtonStyle">?android:buttonBarButtonStyle</item>
        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
        <item name="android:textStyle">bold</item>
        <!--<item name="background">@color/colorPrimary</item>-->
        <!--<item name="android:popupBackground">@color/colorPrimaryDark</item>-->
        <!--<item name="android:textColor">@android:color/white</item>-->
    </style>
</resources>
